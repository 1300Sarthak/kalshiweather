# wedge — Weather Edge Scanner

Find mispricings between professional multi-model weather forecasts and Kalshi
prediction-market prices. Scanner only — no order placement.

Full docs land in Phase 5. Quick start:

```bash
uv venv && uv pip install -e .
wedge forecast --city NYC --days 5
```
